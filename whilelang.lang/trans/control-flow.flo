module control-flow

cfg Root(st) = entry -> cfg st -> exit

cfg Assign(_, e) = entry -> e -> exit

cfg Seq(s1, s2) = entry -> cfg s1 -> cfg s2 -> exit

cfg IfThenElse(c, t, e) =
  entry -> c -> cfg t -> exit,
           c -> cfg e -> exit

cfg While(c, b) = entry -> c -> cfg b -> c -> exit